# Metadata

Place small, redistributable metadata tables used by the final manuscript here. Do not add private identifiers or large primary sequencing files. Record the provenance and upstream release/commit of each metadata table in `docs/DATASETS.md`.
